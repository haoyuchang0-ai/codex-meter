{
  "name": "codex-meter",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight macOS floating window for Codex quota.",
  "main": "server.js",
  "scripts": {
    "build:mac": "zsh scripts/build-floating-window.sh",
    "start": "node server.js",
    "install:hooks": "node scripts/install-activity-hooks.js",
    "install:autostart": "zsh scripts/manage-autostart.sh install",
    "uninstall:autostart": "zsh scripts/manage-autostart.sh uninstall",
    "test": "node --test test/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/haoyuchang0-ai/codex-meter.git"
  },
  "engines": {
    "node": ">=18"
  }
}
